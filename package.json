{
  "name": "diagram-engine",
  "private": true,
  "version": "0.0.0",
  "description": "Prompt-driven architecture diagramming engine — local agent architecture, no API keys",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspaces",
    "check:no-model-sdk": "node scripts/check-no-model-sdk.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^3.0.0"
  }
}
